#include<stdio.h>
#include<math.h>
void power(int,int)
void main()
{
    int a,b;
    printf("enter the value of a and b");
    scanf("%d %d",&a,&b);
    powr=power(a,b);
}
power(a,b)
{
    if(b==0)
        return 1;

}
